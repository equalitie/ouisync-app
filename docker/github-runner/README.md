# Dockerized Github Actions runner

This directory contains a Dockerfile and some supporting scripts for running the [Github Actions
Runner](https://github.com/actions/runner) in a docker container.

## Usage

Copy this directory to a machine that should host the runner and run `run.sh` (try `run.sh --help`
for more info).